# Checkpoint 2026-09-24 — Plan A: DESIGN.md Gap 1 migration (173 sites → @theme tokens)

## Goal (user: "Продолжай! DESIGN.md готов? используешь? действуй стеками планов")
Plan stack agreed:
- **Plan A (DONE this turn)**: migrate the 173 `text-[8-11px]` + 2 `bg-[#hex]` sites to the
  DESIGN.md `@theme` tokens, zero visual delta, full verification stack.
- **Plan B (NEXT)**: design-qa / component-library-alignment / ui-alignment-review sweep of
  the app AGAINST `DESIGN.md` as the contract (marketplace skills never yet used for this).
- **Plan C (queued)**: Perf #3 full (palettesProcedural idle deferral, preserve the
  `ALL_COLOR_PALETTES[0]` boot invariant!) + Perf #4 full (shader monolith dynamic import —
  either await-import in `WebGLEngine.init` or build-time per-fractal splice; ShaderManager v3
  currently needs the whole `FRAGMENT_SHADER_SOURCE` at init).

## Done (Plan A)
- `src/index.css`: micro tiers dropped their `--text-*--line-height` partners (would have
  changed every line's computed lh; `text-[Npx]` emits font-size ONLY). Comment documents why.
- 173 sites in 10 components → `text-micro` (81) / `text-nano` (63) / `text-micro-sm` (22) /
  `text-micro-xs` (7); `bg-[#06050b]`→`bg-surface-loader`, `bg-[#090812]`→`bg-surface-modal`.
  Residue in `src/**`: text-[8-11px]=0, bg-[#hex]=0.
- Zero-delta PROOF: `tests/css-equiv-probe.mjs` (versioned; JSON evidence in gitignored
  `tests/results/`) — computed
  (fontSize,lineHeight) multiset over all direct-text nodes, 7 UI states, fixed boot URL
  `/?test=1#type=mandelbulb&renderStyle=solid`. before vs after1: **IDENTICAL** (1,225 nodes,
  20 pairs); after1 vs after2 identical (deterministic). JSONs in `tests/results/equiv-*.json`.
- Verification stack: `tsc` 0 · build ok (boot 282.9 KB, CSS 82.19→81.84 KB — earlier "80.5 KB"
  checkpoint figure was stale; measured HEAD build is 82.19) · `npm run test` 1875/0 ·
  `test:quality` pass (pre-existing length warnings) · `test:mobile` PASS @4175.
- Docs reconciled: `DESIGN.md` (Overview fact 1, typography lh-intent note, Gap 1 "fully
  migrated", evidence range → e18acbe), `docs/DESIGN_DEBT.md` (Bucket 2 RESOLVED, "25 sites"
  corrected to 173, verdict = historical baseline), `docs/index-css.md` (migration complete),
  `docs/CHANGELOG.md` (Unreleased/Design system entry).

## Probe gotchas (relearned the hard way)
- Headless: needs launch args `--no-sandbox --enable-gpu-rasterization --use-gl=angle` and a
  90s gate for software-WebGL shader compile (loader dismissed only then) — same recipe as
  `tests/mobile-design-audit.ts`. Plain goto+timeout sees only the 6-node loader DOM.
- Atlas modal does NOT close on Escape via page.keyboard (header stayed up and blocked the
  next click); use `#btn-atlas-modal-close`. ControlsPanel is `top-3 right-3 z-40` — it
  covers the HUD menu; close via `#close-engineer-panel-btn` before clicking `#top-right-trigger`.
- Modal open IDs: profile `#open-user-profile-btn` (needs `#top-right-trigger` first),
  atlas `#open-atlas-modal-btn` (ControlsPanel header), explanation `#show-math-info-btn`,
  manifest `#open-manifest-from-profile-btn` (inside profile).

## State / housekeeping
- Git root: `J:\project\swype-imagine\app`. Before this turn HEAD `e18acbe` == origin/main.
- `vite preview --port 4175` still running in background terminal 2 (kill after commit).
- `verification/` at workspace root: DO NOT TOUCH (standing user constraint).
- CI still blocked by GitHub Actions billing lock (external, not code).
- Commit this turn: `design(#1): migrate all 173 arbitrary text sites to @theme tokens` (+docs).

## Next step (concrete)
Run Plan B: `design-qa` skill over DESIGN.md vs the migrated app (frontmatter token refs now
match reality); then component-library-alignment. Then decide with user whether Plan C ships
before or after the design-qa sweep.
